<%= form_with url: import_mountains_path, method: :post do |f| %>
  <div class="container">
    <%= f.file_field :file, accept: ".csv" %>
    <div class="btn-group">
      <%= f.submit "Upload Mountains", class: "btn btn-primary" %>
    </div>
  </div>
<% end %>