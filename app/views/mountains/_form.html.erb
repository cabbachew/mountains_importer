<%= form_with(model: mountain) do |form| %>
  <% if mountain.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mountain.errors.count, "error") %> prohibited this mountain from being saved:</h2>

      <ul>
        <% mountain.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :height, style: "display: block" %>
    <%= form.text_field :height %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
